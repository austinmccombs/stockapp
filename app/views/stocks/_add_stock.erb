<%= form_with(model: stock, local: true) do |form| %>
  <% if stock.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock.errors.count, "error") %> prohibited this stock from being saved:</h2>

      <ul>
      <% stock.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <table class='table'>
    <tr>
      <td>
        <div class="field">
         <center> <%= form.text_field :ticker, placeholder: 'Ticker' %></center>
        </div>
      </td>
    </tr>

        <div class="field">
         <center> <%= form.number_field :user_id, value: current_user.id, type: 'hidden' %> </center>
          
        </div>
    <tr>
      <td>
        <div class="actions">
         <center> <%= form.submit 'Add Stock' %> </center>
        </div>
      </td>
    </tr>
  </table>
<% end %>
